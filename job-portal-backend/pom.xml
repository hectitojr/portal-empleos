<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Parent: BOM de Spring Boot -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.6</version>
        <relativePath/>
    </parent>

    <!-- Raíz: padre y agregador -->
    <groupId>com.zoedatalab.empleos</groupId>
    <artifactId>job-portal-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>job-portal-backend</name>
    <description>Backend Portal de Empleos - Monolito Modular (Hexagonal + DDD + TDD)</description>

    <!-- Propiedades globales -->
    <properties>
        <java.version>21</java.version>

        <mapstruct.version>1.6.2</mapstruct.version>

        <!-- Seguridad -->
        <logback.version>1.5.19</logback.version>
        <postgresql.version>42.7.8</postgresql.version>
        <commons-compress.version>1.26.0</commons-compress.version>

        <!-- Persistence helpers -->
        <hibernate-types.version>2.21.1</hibernate-types.version>

        <!-- Testing (BOM import) -->
        <testcontainers.version>1.20.6</testcontainers.version>

        <!-- FIX: CVE-2025-48924 -->
        <commons-lang3.version>3.18.0</commons-lang3.version>

        <lombok.version>1.18.34</lombok.version>
        <lombok.mapstruct.binding.version>0.2.0</lombok.mapstruct.binding.version>

        <flyway.version>11.7.2</flyway.version>
        <jjwt.version>0.12.6</jjwt.version>

        <!-- Build/test -->
        <bytebuddy.version>1.17.7</bytebuddy.version>
        <surefire.version>3.2.5</surefire.version>
        <failsafe.version>3.2.5</failsafe.version>

        <!-- JaCoCo -->
        <jacoco.version>0.8.12</jacoco.version>
        <jacoco.skip>false</jacoco.skip>

        <argLine/>
        <spring-boot.run.skip>true</spring-boot.run.skip>
    </properties>

    <!-- Módulos -->
    <modules>
        <module>modules/common</module>
        <module>modules/catalogs</module>
        <module>modules/iam</module>
        <module>modules/applicants</module>
        <module>modules/companies</module>
        <module>modules/jobs</module>
        <module>modules/applications</module>
        <module>modules/moderation</module>
        <module>modules/notifications</module>
        <module>modules/persistence-jpa</module>
        <module>modules/api</module>
    </modules>

    <!-- BOMs / dependencyManagement -->
    <dependencyManagement>
        <dependencies>

            <!-- Versionado centralizado de módulos internos del monolito -->
            <dependency>
                <groupId>com.zoedatalab.empleos</groupId>
                <artifactId>job-portal-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zoedatalab.empleos</groupId>
                <artifactId>job-portal-catalogs</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zoedatalab.empleos</groupId>
                <artifactId>job-portal-iam</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zoedatalab.empleos</groupId>
                <artifactId>job-portal-applicants</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zoedatalab.empleos</groupId>
                <artifactId>job-portal-companies</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zoedatalab.empleos</groupId>
                <artifactId>job-portal-jobs</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zoedatalab.empleos</groupId>
                <artifactId>job-portal-applications</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zoedatalab.empleos</groupId>
                <artifactId>job-portal-moderation</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zoedatalab.empleos</groupId>
                <artifactId>job-portal-notifications</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zoedatalab.empleos</groupId>
                <artifactId>job-portal-persistence-jpa</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- JJWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
            </dependency>

            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>com.vladmihalcea</groupId>
                <artifactId>hibernate-types-60</artifactId>
                <version>${hibernate-types.version}</version>
            </dependency>

            <!-- Seguridad: Logback -->
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-core</artifactId>
                <version>${logback.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
            </dependency>

            <!-- PostgreSQL JDBC -->
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>${postgresql.version}</version>
            </dependency>

            <!-- Commons Compress -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-compress</artifactId>
                <version>${commons-compress.version}</version>
            </dependency>

            <!-- commons-lang3 transitivo -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>

            <!-- MapStruct -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- Infra -->
            <dependency>
                <groupId>org.codehaus.janino</groupId>
                <artifactId>janino</artifactId>
                <version>3.1.12</version>
            </dependency>

            <dependency>
                <groupId>org.yaml</groupId>
                <artifactId>snakeyaml</artifactId>
                <version>2.4</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <!-- Dependencias heredadas por todos los módulos -->
    <dependencies>

        <dependency>
            <groupId>net.bytebuddy</groupId>
            <artifactId>byte-buddy-agent</artifactId>
            <version>${bytebuddy.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Stack de pruebas para TODOS los módulos (JUnit 5 + Mockito + AssertJ) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.junit.vintage</groupId>
                    <artifactId>junit-vintage-engine</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>5.20.0</version>
            <scope>test</scope>
        </dependency>

        <!-- API de validación para compilar anotaciones (@Size, etc.) en todos los módulos -->
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
    </dependencies>

    <!-- Build global -->
    <build>

        <!-- Versiones centralizadas de plugins -->
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>

            <!-- JaCoCo: cobertura por módulo + gate en verify -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <configuration>
                    <skip>${jacoco.skip}</skip>
                </configuration>
                <executions>

                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <propertyName>argLine</propertyName>
                        </configuration>
                    </execution>

                    <!-- Report por módulo -->
                    <execution>
                        <id>jacoco-report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>

                    <!-- Gate de cobertura -->
                    <execution>
                        <id>jacoco-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <!-- Exclusiones coherentes con DDD/Hexagonal -->
                            <excludes>
                                <exclude>**/config/**</exclude>
                                <exclude>**/security/**</exclude>
                                <exclude>**/dto/**</exclude>
                                <exclude>**/entity/**</exclude>
                                <exclude>**/mapper/**</exclude>
                                <exclude>**/*Application*</exclude>
                                <exclude>**/*BeansConfig*</exclude>
                                <exclude>**/*Config*</exclude>
                                <exclude>**/*Properties*</exclude>
                                <exclude>**/generated/**</exclude>
                            </excludes>

                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>INSTRUCTION</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.70</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.60</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>

                </executions>
            </plugin>

            <!-- Compilación -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <release>${java.version}</release>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>${lombok.mapstruct.binding.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-maven-plugin</artifactId>
                <version>${flyway.version}</version>

                <configuration>
                    <locations>
                        <location>filesystem:modules/persistence-jpa/src/main/resources/db/migration</location>
                    </locations>

                    <cleanDisabled>false</cleanDisabled>
                </configuration>

                <dependencies>
                    <!-- Soporte PostgreSQL para el plugin Maven de Flyway -->
                    <dependency>
                        <groupId>org.flywaydb</groupId>
                        <artifactId>flyway-database-postgresql</artifactId>
                        <version>${flyway.version}</version>
                    </dependency>

                    <!-- Driver JDBC PostgreSQL -->
                    <dependency>
                        <groupId>org.postgresql</groupId>
                        <artifactId>postgresql</artifactId>
                        <version>${postgresql.version}</version>
                    </dependency>
                </dependencies>
            </plugin>

            <!-- Surefire (unit tests) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.version}</version>
                <configuration>
                    <argLine>
                        ${argLine}
                        -XX:+EnableDynamicAgentLoading
                        -Xshare:off
                    </argLine>
                    <systemPropertyVariables>
                        <spring.profiles.active>test</spring.profiles.active>
                    </systemPropertyVariables>
                    <useModulePath>false</useModulePath>
                </configuration>
            </plugin>

            <!-- Failsafe (integration tests) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${failsafe.version}</version>

                <configuration>
                    <argLine>
                        ${argLine}
                        -XX:+EnableDynamicAgentLoading
                        -Xshare:off
                    </argLine>
                    <useModulePath>false</useModulePath>
                </configuration>

                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Maven Enforcer -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>

                <configuration>
                    <fail>true</fail>
                    <rules>
                        <dependencyConvergence/>
                        <requireUpperBoundDeps/>
                        <bannedDependencies>
                            <searchTransitive>true</searchTransitive>
                            <excludes>
                                <exclude>ch.qos.logback:logback-core:(,1.5.19)</exclude>
                                <exclude>ch.qos.logback:logback-classic:(,1.5.19)</exclude>
                                <exclude>org.postgresql:postgresql:(,42.7.7)</exclude>
                                <exclude>org.apache.commons:commons-compress:(,1.26.0)</exclude>
                                <exclude>org.apache.commons:commons-lang3:(,3.18.0)</exclude>
                            </excludes>
                        </bannedDependencies>
                    </rules>
                </configuration>

                <executions>
                    <execution>
                        <id>enforce-secure-deps</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

</project>